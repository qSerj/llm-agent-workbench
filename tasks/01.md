# Task 1 — Repository discovery

Create `docs/01-interleavers.md`. Document, based only on repository evidence: the public profile abstraction; every concrete implementation; validation limits; exactly how each profile is encoded by `ToRegisters()`; and at least one real usage site.

Rules: inspect the repository yourself; do not modify `src/**`; only edit `docs/**`; do not invent hardware behavior; mark unknowns as unknown; re-check source before finishing.
