# Task 2 — Trace real behavior

Create `docs/02-apply-behavior.md`. Determine exact observable behavior of applying profiles through the controller. Answer: first application; repeated same encoded profile; whether equality is identity/type/encoded registers; command value; when cache updates relative to transport; what follows if transport write throws.

Rules: use only repository evidence; find definitions/usages yourself; do not modify `src/**`; only edit `docs/**`; do not infer undocumented hardware semantics.
